# vim:ft=dosini
# Add this to your client config. In your crontab, add
#
# 0 * * * * /usr/local/bin/zabbix-check-needs-reboot.sh --cron
#
UserParameter=custom.rebootcheck,/usr/local/bin/zabbix-check-needs-reboot.sh --local
